import React from "react";

const Options = ({ children }) => {
    return (
        <div>
            <h1>Hello world Options</h1>
            {children}
        </div>
    )
}

export default Options;